name: "CodeQL"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  analyze:
    name: Analyze
    uses: s3ntin3l8/.github/.github/workflows/codeql.yml@main
    with:
      languages: "go,javascript-typescript"
    permissions:
      actions: read
      contents: read
      security-events: write
